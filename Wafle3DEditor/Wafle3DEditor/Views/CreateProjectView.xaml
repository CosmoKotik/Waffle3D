<UserControl x:Class="Wafle3DEditor.Views.CreateProjectView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        
        xmlns:geminibehaviors="clr-namespace:Gemini.Framework.Behaviors;assembly=Gemini" 
        xmlns:behaviors="http://schemas.microsoft.com/expression/2010/interactivity"
        mc:Ignorable="d" 
        d:DesignHeight="300" d:DesignWidth="500"
        Focusable="True">

    <Grid>
        <Grid.Background>
            <SolidColorBrush Color="{Binding Background, FallbackValue=White}"/>
        </Grid.Background>

        <Label Content="{Binding PrjName}"/>
        <Label Content="{Binding PrjPath}"/>
        
    </Grid>
</UserControl>
